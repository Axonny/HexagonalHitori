# HexagonalHitori

```
usage: main.py [-h] [-M M] filename

positional arguments:
  filename    Path to file

options:
  -h, --help  show this help message and exit
  -M M        Count graph components
```